language: node_js
node_js:
    - "8"

script:
    - wget https://dl.itch.ovh/butler/linux-amd64/head/butler?t=$(date +%s) -O butler
    - chmod +x butler
    - export PATH="$PATH:$PWD"
    - butler -V
    - export DEBUG="*"
    - npm test
